"""HTTP routers. main.py includes every module in here that defines `router`.

An overlay adds an endpoint by adding a file to this package. Nothing in
main.py changes, which is what keeps optional features conditional paths
rather than conditional blocks (Loftline invariant 6).
"""
