# {{ project_name }}

What this project is, in a paragraph, for whoever opens it next. Generated by
Loftline from the spec below; the dashboard shows this file on the project's
page, so keep it short and keep it true.

**Stack.** A FastAPI backend on {% if database == 'aura' %}Neo4j{% else %}Postgres{% endif %}{% if web %}, a Vite and React site{% endif %}{% if mobile %}, an Expo app for iOS and Android{% endif %}{% if notifications %}, push notifications{% endif %}{% if payments %}, payments ({{ payments | join(', ') }}){% endif %}. Hosted on Render{% if domain %} at {{ domain }}{% endif %}.

**Environments.** {{ environments | join(', ') }}. Three long-lived branches: `dev` for work, `staging` and `prod` for deploying, promoted by pull request.

**Where things are.** The API in `api/`{% if web %}, the site in `web/`{% endif %}{% if mobile %}, the app in `app/`{% endif %}, the repository's own infrastructure in `infra/`. `README.md` says how to run it; `.copier-answers.yml` says what it was generated with.
