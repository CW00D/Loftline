# Written by Loftline from the project spec. Tracked: it holds names, never
# values. Credentials reach Terraform through the environment (GITHUB_TOKEN).
repository   = "{{ project_name }}"
description  = "Generated by Loftline"
# GitHub environments hold secrets and cost nothing, so both exist from the
# start: the day production is added to the spec, its environment is
# already there to receive them. Render resources follow the spec's list.
environments = ["staging", "production"]
