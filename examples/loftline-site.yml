# The Loftline website: accounts, membership, and the download for members.
# Staging only for now, which keeps every Render resource on a free plan;
# add production to the list when it is time to pay for its database.
project_name: loftline-site
package_name: loftline_site
database: postgres
web: true
payments: [subscriptions]
hosting:
  api: render
  web: render
environments: [staging]
